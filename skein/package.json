{
  "name": "kalgebra-skein-applet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Skein · triangulations — an ideal-triangulation editor for A_q[T[A1, Sigma]] (KAlgebra Applet Suite, Plan 39). Light path: input + flips + BPS-quiver export.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0"
  }
}
